{
    "type": "object",
    "properties": {
        "name": {
            "title": "${strings.common.displayName}",
            "type": "string",
            "pattern": "^[^\\\\\\\\/:*?\\\\\\\"<>|]*$"
        },
        "projectType": {
            "title": "${strings.common.projectType}",
            "type": "string"
        }
    },
    "required": [
        "name",
        "projectType"
    ]
}
